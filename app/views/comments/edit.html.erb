<h1>Edit Comment!</h1>

<%= render partial: "layouts/errors", locals: {object: @comment} %>

<%= form_for(@comment) do |f|%>

  <%= f.label :experience %>
  <%= f.collection_select :experience_id, Experience.all, :id, :title %>

  <%= f.label :content %>
  <%= f.text_field :content %>


  <%= f.submit %>

<% end %>